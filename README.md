# atom-development-list

It's my installed packages for the Atom editor.

# Prerequisites
* Atom package manager(https://atom.io/)
* Open Atom > Preferences > Install Shell Commands

# Installation
Command line:
`apm install --packages-file ~/path/to/this/file.txt`
